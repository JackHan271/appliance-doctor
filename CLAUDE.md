# 项目工作说明（CLAUDE.md）

本文件是「家电故障诊断多 Agent」项目的工作指引，供 AI 协作与后续维护使用。所有开发必须遵守本文件约定。

## 项目一句话

面向冰箱/洗衣机的多 Agent 家电故障诊断 Web 应用：文字/照片提问 → LangGraph 诊断状态机 → 三级安全护栏 → 详细维修讲解。

## 标准文件路径（唯一事实来源）

- 需求文档：`docs/01-需求文档.md`
- 技术选型：`docs/02-技术选型.md`
- 设计规范：`docs/03-设计规范.md`
- 执行计划：`docs/04-执行计划.md`
- 数据策略：`docs/05-数据策略.md`

## 开发日志路径

- 待办事项：`devlog/TODO.md`
- 已完成事项：`devlog/DONE.md`

## 工作约定（必须遵守）

1. **按序推进**：严格按 `docs/04-执行计划.md` 的阶段顺序推进，一次只做一个阶段，不跨阶段并行，不一口气做太多。
2. **决策归用户**：所有关键技术选型按「优势 vs 劣势」对比呈现给用户，由用户拍板后再落地。
3. **同步日志**：每完成一项开发事项，立即更新 `devlog/DONE.md`（追加完成项）与 `devlog/TODO.md`（勾掉/新增待办）。
4. **代码与数据分离**：数据文件（手册语料、图片）不入代码仓库、不随代码分发（见 `docs/05-数据策略.md`）。
5. **安全红线**：严格遵守 `docs/03-设计规范.md` 的三级安全护栏；红色等级绝不输出危险维修步骤。
6. **评估先行**：Agent 逻辑完成后、前端之前，先建立评估集与指标（见执行计划 Phase 4）。

## 当前状态

需求与技术选型已全部锁定（2026-09-19）。Phase 0/1 完成，Phase 2 骨架已搭（FastAPI + Claude + BGE + Milvus），待本机验证后继续 MCP 工具集 / RAG / LangGraph 图组装。
