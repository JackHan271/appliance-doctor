# 家电故障诊断多 Agent · 项目恢复指南

> 用途：卸载重装 Claude 前后的备份与恢复。本文件是自包含的「项目快照 + 恢复手册」，重装后照着走即可无缝续接。

## 〇、重装前（先做这一步）

本文件夹（`Desktop\appliance-doctor`）就是完整备份。原项目位于：
`C:\Users\ASUS\AppData\Local\Claude-3p\local-agent-mode-sessions\24c728f7\00000000\24f5dccf\outputs\appliance-doctor`

卸载前请再次确认本桌面文件夹完整存在（含 docs/、data/、backend/、devlog/）。

## 一、重装后要做的事（checklist）

1. 打开本文件夹，确认 `docs/`、`data/`、`backend/`、`devlog/` 都在。
2. 重新打开 Claude，让它读 `CLAUDE.md` 与 `docs/` 恢复上下文（或直接说「继续推进 appliance-doctor 项目，项目在桌面」）。
3. 验证沙箱：让 Claude 跑一个简单命令（如 `echo ok`）。沙箱恢复 → 回到「Claude 直接跑代码」模式；未恢复 → 继续「Claude 写代码 + 你本机跑」模式。
4. 按本文件「当前进度」从断点继续。

## 二、当前进度与下一步

已完成：Phase 0（目录 + 文档）、Phase 1（数据：三品牌故障码 + 型号种子 + 手册采集脚本）、Phase 2 骨架（FastAPI + Claude + BGE + Milvus + 查故障码工具 + LangGraph 状态）。

下一步：本机验证骨架可跑（uvicorn）→ MCP 工具集（读铭牌 / 识别现象 / 查安全手册）→ RAG 检索 → LangGraph 图组装（诊断 Agent + 安全审查 Agent）→ 评估集（Phase 4）→ 前端（Phase 5）。

## 三、已确认需求与决策

| 决策点 | 结论 |
|--------|------|
| 产品定位 | 家电故障维修软件，面向市面常用 10 品牌冰箱 + 洗衣机（MVP 先 3 品牌） |
| 核心功能 | 文字/照片提问 → 严格保证安全 → 详细讲解维修 |
| 次要功能 | 个人家电名单 / 维修历史记录 / 通用安全手册 |
| UI | 四区域模块（提问交互区最大），主色淡蓝 |
| 运行形态 | Web 应用（React + Docker，可加 PWA） |
| 安全边界 | 三级安全护栏（绿=可自修 / 黄=谨慎+断电 / 红=必须找师傅），安全审查 Agent 把关 |
| 品牌范围 | 三品牌主力（海尔/美的/西门子）+ 格力后备 |
| 照片识别 | 故障现象识别 + 型号铭牌识别 |
| 技术路线 | 调用 LLM + RAG + 工具调用 + 多 Agent，不微调 |

## 四、技术选型（全部锁定）

| 领域 | 选择 |
|------|------|
| Agent 编排 | LangGraph |
| 工具协议 | MCP |
| 多 Agent | 诊断 Agent + 安全审查 Agent |
| 向量数据库 | Milvus（Lite / Standalone，pymilvus） |
| 后端 | FastAPI |
| 前端样式 | Tailwind CSS |
| 业务数据库 | PostgreSQL |
| LLM | Claude（文本 + 视觉） |
| Embedding | BGE 本地（bge-small-zh-v1.5） |

## 五、执行计划（9 阶段）

0 初始化 → 1 数据层 → 2 核心后端 → 3 Agent 编排 → 4 评估体系 → 5 前端 UI → 6 次要功能 → 7 部署打磨 → 8 扩展 10 品牌。

## 六、数据策略与合规

故障码=事实性技术参数可自由收集；说明书/手册仅本地 RAG 用、不公开分发不商用；图片优先自拍/CC0/合成；开源时代码与数据分离。

## 七、网络环境提示（大陆）

- pip 用清华镜像：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`
- BGE 模型下载走镜像：PowerShell 里 `$env:HF_ENDPOINT="https://hf-mirror.com"`
- Claude API 需代理/VPN。

## 八、项目结构

```
appliance-doctor/
  CLAUDE.md          # 工作约定与文件路径指引
  docs/              # 01需求 02技术选型 03设计规范 04执行计划 05数据策略
  devlog/            # TODO.md / DONE.md
  data/              # fault_codes/ models/ manuals/
  backend/           # FastAPI + Claude + BGE + Milvus 骨架
```

## 九、关键约定（来自 CLAUDE.md）

1. 按执行计划顺序推进，一次一个阶段。
2. 技术决策按「优势/劣势」对比呈现，用户拍板。
3. 每完成一项同步更新 devlog 的 TODO/DONE。
4. 代码与数据分离。
5. 三级安全护栏红线，红色绝不输出危险步骤。
6. 评估先行（Agent 完成后、前端之前建评估集）。
